import React from "react";
import ReactDOM from "react-dom/client";
import HaulBoard from "./haulboard.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <HaulBoard />
  </React.StrictMode>
);
